# 📝 Простой сервис заметок

Простое одностраничное веб-приложение для управления заметками с использованием HTML, CSS (Bootstrap) и JavaScript.

## 📋 1. Функциональность сервиса

Сервис предоставляет пользователям возможность создавать, редактировать, просматривать и удалять текстовые заметки. Основные возможности:

- ✅ **Создание заметок**: Пользователь может создать новую заметку с заголовком и текстовым содержимым
- ✏️ **Редактирование заметок**: Существующие заметки можно отредактировать в любое время
- 🗑️ **Удаление заметок**: Заметки можно удалять с подтверждением действия
- 💾 **Автосохранение**: Все заметки автоматически сохраняются в локальное хранилище браузера
- 📱 **Адаптивный дизайн**: Интерфейс корректно отображается на устройствах с разным размером экрана
- 🌙 **Темная тема**: Приложение использует темную цветовую схему для комфортной работы

**🌐 Демонстрационная версия**: [https://xx-arteeem-xx.github.io/mk2-test-repo/](https://xx-arteeem-xx.github.io/mk2-test-repo/)

## 🏗️ 2. Архитектура приложения

Приложение построено как одностраничное веб-приложение (SPA) без серверной части:

- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **🎨 Стилизация**: Bootstrap 5.3 с темной темой, Font Awesome для иконок
- **💽 Хранение данных**: Используется браузерный localStorage для сохранения заметок между сессиями
- **🗄️ Архитектура данных**: 
  - Каждая заметка представлена объектом с полями: `id`, `title`, `content`
  - Данные сериализуются в JSON для хранения в localStorage
  - Уникальные идентификаторы генерируются на основе временных меток

Ключевые функции приложения:
- `loadNotesFromStorage()` - загрузка заметок из localStorage
- `saveNotesToStorage()` - сохранение заметок в localStorage
- `renderNotes()` - отрисовка списка заметок в интерфейсе
- `saveNote()` - обработка создания/редактирования заметок
- `deleteNote()` - удаление заметок с подтверждением

## 🚀 3. Локальная установка и развертывание

### ⚙️ Установка нативно (без Docker)

1. **📥 Клонирование репозитория**:
   ```bash
   git clone https://github.com/xx-arteeem-xx/mk2-test-repo.git
   cd mk2-test-repo
   ```

2. **🌐 Использование простого HTTP-сервера**:

   **Python 3**:
   ```bash
   python3 -m http.server 8080
   ```

   **Node.js с http-server**:
   ```bash
   npx http-server . -p 8080
   ```

3. **🐧 Установка с помощью nginx**:

   Установите nginx:
   ```bash
   # Ubuntu/Debian
   sudo apt update
   sudo apt install nginx
   
   # CentOS/RHEL
   sudo yum install nginx
   ```

   Создайте конфигурацию:
   ```bash
   sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/notes-app
   sudo nano /etc/nginx/sites-available/notes-app
   ```

   Пример конфигурации:
   ```nginx
   server {
       listen 80;
       server_name localhost;
       root /path/to/mk2-test-repo;
       index index.html;
       
       location / {
           try_files $uri $uri/ =404;
       }
   }
   ```

   Активируйте конфигурацию и перезапустите nginx:
   ```bash
   sudo ln -s /etc/nginx/sites-available/notes-app /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl restart nginx
   ```

### 🐳 Установка с помощью Docker

1. **📄 Создайте Dockerfile**:
   ```dockerfile
   FROM nginx:alpine
   COPY index.html /usr/share/nginx/html/
   EXPOSE 80
   ```

2. **🔨 Соберите и запустите контейнер**:
   ```bash
   docker build -t notes-app .
   docker run -d -p 8080:80 --name notes-app-container notes-app
   ```

3. **🎛️ Или используйте Docker Compose**:
   ```yaml
   version: '3.8'
   services:
     web:
       image: nginx:alpine
       ports:
         - "8080:80"
       volumes:
         - ./index.html:/usr/share/nginx/html/index.html
   ```

## 🌐 4. Процесс деплоя

Приложение использует GitHub Pages для автоматического деплоя:

1. **⚙️ Конфигурация**: Репозиторий настроен для использования GitHub Pages с ветки `main`
2. **🔄 Автоматический деплой**: При каждом коммите в ветку `main` происходит автоматическая сборка и деплой на GitHub Pages
3. **🔗 URL приложения**: Приложение доступно по адресу `https://xx-arteeem-xx.github.io/mk2-test-repo/`
4. **🔧 Настройка**: Для активации GitHub Pages перейдите в Settings → Pages и выберите branch: `main` и folder: `/root`

**📋 Процесс обновления**:
1. Внесите изменения в код
2. Зафиксируйте изменения и отправьте в ветку `main`
3. GitHub Actions автоматически выполнит деплой
4. Через 1-2 минуты изменения отобразятся на сайте

## 👨‍💻 5. Руководство для разработчиков

### 🔄 Процесс разработки

Разработка ведется по методологии, основанной на задачах (issue-driven development):

1. **📝 Создание задачи (Issue)**:
   - Каждая новая функциональность или исправление начинается с создания Issue в GitHub
   - В Issue подробно описывается: проблема, требования, критерии приемки

2. **🌿 Создание ветки**:
   - Используя плагин GitHub для VS Code, выберите соответствующее Issue
   - Создайте ветку на основе Issue (название ветки должно соответствовать шаблону: `feature/issue-{номер}` или `fix/issue-{номер}`)
   - Ветка автоматически связывается с Issue

3. **🔧 Разработка**:
   - Все коммиты делаются в созданную ветку
   - Сообщения коммитов должны быть четкими и описывать изменения
   - Регулярно синхронизируйте ветку с `main` для избежания конфликтов

4. **🧪 Тестирование**:
   - Тестируйте изменения локально перед созданием Pull Request
   - Убедитесь, что код соответствует стандартам качества

5. **🔍 Создание Pull Request**:
   - Создайте Pull Request из вашей ветки в ветку `main`
   - В описании PR укажите ссылку на Issue (используя ключевые слова: `Closes #{номер}` или `Fixes #{номер}`)
   - Добавьте ревьюверов для код-ревью

6. **✅ Слияние и закрытие Issue**:
   - После одобрения PR и прохождения всех проверок, выполните слияние
   - Issue автоматически закроется после слияния PR в `main`

### 📐 Стандарты кода

- Используйте отступы в 4 пробела для HTML, CSS и JavaScript
- 📝 Комментируйте сложные участки кода
- 🧹 Следуйте принципам чистого кода (DRY, KISS)
- 🌐 Тестируйте изменения в нескольких браузерах

### 📁 Структура проекта

```
mk2-test-repo/
├── index.html          # 📄 Основной файл приложения
└── README.md          # 📖 Документация
```